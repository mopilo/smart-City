import 'package:flutter/material.dart';

class ActiveParkPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('active park')
      ),
    );
  }
}