import 'package:flutter/material.dart';

class ParkHistory extends StatelessWidget{
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Park history')
      ),
    );
  }
}